# Smithery.ai configuration for Oyemi MCP Server
# https://smithery.ai/docs/config

startCommand:
  type: "stdio"
  configSchema:
    type: "object"
    properties: {}
  commandFunction: |-
    (config) => ({ command: "oyemi-mcp", args: [] })
